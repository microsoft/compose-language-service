# Compose Language Client Test Extension

A super simple extension to act as a client for the language server, to facilitate easy testing.

Launch with the `Client + Server` compound launch configuration.
