version: '3.8'

services:
  first:
    image: alpine:latest
    entrypoint: tail -f /dev/null
    volumes:
      - ~:/homedir:rw
    ports:
      - 8080:80
    environment:
      - FOO=BAR
